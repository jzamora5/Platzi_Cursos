const config = {
  clientIdPaypal: String(process.env.CLIENT_ID_PP),
  googleMapsApiKey: String(process.env.API_KEY_GOOGLE_MAPS),
  positionStackApiKey: String(process.env.POSITION_STACK_API_KEY),
};

export default config;
